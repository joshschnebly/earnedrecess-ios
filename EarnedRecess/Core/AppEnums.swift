import Foundation

enum AppMode: String, CaseIterable { case standard, writeToWatch, both }
enum LetterCase: String, CaseIterable { case uppercase, lowercase, both }
enum TemplateStyle: String, CaseIterable { case solid, dotted, none }
